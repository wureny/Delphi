# Copy to .env.adk.local and source it before running ADK runtime.

# Required for provider=openai
OPENAI_API_KEY=your_llm_api_key

# Optional for OpenAI-compatible gateways (must include /v1 suffix)
# OPENAI_API_BASE=https://api.openai.com/v1

# Optional persistent ADK session store
# Example: sqlite+aiosqlite:///tmp/delphi_adk_sessions.db
# DELPHI_ADK_SESSION_DB_URL=sqlite+aiosqlite:///tmp/delphi_adk_sessions.db
